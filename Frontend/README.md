# Frontend (Vite + React)

3D shirt customizer UI. Repository overview and Cloudinary notes live in the [root README](../README.md).

## Quick start

```bash
npm install
npm run dev
```

From the monorepo root, use `cd Frontend` first.
